import React from 'react';

/**
 * MultiElement component returns multiple elements wrapped in a React Fragment.
 */
const MultiElement = () => {
  // Using React.Fragment (or shorthand <>) to return multiple elements.
  return (
    <>
      <h2>Title</h2>
      <p>Description text here.</p>
    </>
  );
};

export default MultiElement;
