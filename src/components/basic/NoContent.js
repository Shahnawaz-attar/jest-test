import React from 'react';

/**
 * NoContent component deliberately returns null (renders nothing).
 */
const NoContent = () => {
  // Returning null means nothing is rendered to the DOM.
  return null;
};

export default NoContent;
